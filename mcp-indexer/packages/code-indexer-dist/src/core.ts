// The code-graph contract: node/edge/snapshot/status/knowledge schemas, ids, and
// the analysis + projection helpers. Re-exported so consumers of the bundled
// package get the types without depending on the (unpublished) workspace package.
export * from '@repo/code-graph-core';
